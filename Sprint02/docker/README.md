# Docker

This folder is reserved for shared Docker assets that may be introduced in later sprints.

Sprint 1 service builds are defined in each service directory and orchestrated by the root `docker-compose.yml`.
